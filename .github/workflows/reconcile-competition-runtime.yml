name: Reconcile competition runtime

on:
  push:
    branches:
      - "starfie1d/refactor-v2-competition-runtime"

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo reconcile-probe
