# UI 视觉设计

> 所有页面基于深色主题，详细 token 值见 `docs/ui-tokens.md`。

---

## 通用三态规范

每个数据展示区域必须处理以下三态：

| 状态 | 展示方式 |
|---|---|
| Loading | Skeleton 占位（灰色方块，高度与实际内容一致） |
| Empty | 居中图标 + 一行说明文字 + 可选的行动按钮 |
| Error | 红色 Toast 提示 + 页面内 Error UI（不替换整页） |

---

## 首页 `/`

```
┌────────────────────────────────────────────────────────────┐
│  Header                                                    │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────────────────────┐  ┌──────────────────┐   │
│  │  Hero（Tactical Grid 深色）   │  │  CURRENT SEASON  │   │
│  │  [ RIVALHUB / 赛季 SLUG ]    │  │  赛季名称        │   │
│  │  RivalHub                    │  │  [StatusPill][种] │   │
│  │  赛季名（橙色 accent）         │  │  ─────────────── │   │
│  │  简介文字                     │  │  TEAMS PLAYERS   │   │
│  │  [进入赛季→] [报名参赛] [归档] │  │  STAGE（MiniStat）│   │
│  └──────────────────────────────┘  │  [进入赛季→]     │   │
│                                    └──────────────────┘   │
│  ① 入口（2×4 nav tiles，< lg 时 2×4）                      │
│  ┌──────┐┌──────┐┌──────┐┌──────┐                         │
│  │ 报名 ││ 投票 ││ 选秀 ││ 战队 │ ...                      │
│  └──────┘└──────┘└──────┘└──────┘                         │
│                                                            │
│  ② 其他赛季（仅当存在时，1/2/3 列响应）                      │
├────────────────────────────────────────────────────────────┤
│  Footer                                                    │
└────────────────────────────────────────────────────────────┘
```

- Hero 区：左大右小两栏（lg 以下堆叠），Eyebrow mono 标签 + Display 大标题 + 操作按钮组
- CURRENT SEASON 面板：MiniStat 三格（TEAMS / PLAYERS / STAGE），走 `<MiniStat>` 组件
- Nav tiles：`grid-cols-2 lg:grid-cols-4`，8 个固定入口，hover 时 border 变亮
- 其他赛季：`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
- 无活跃赛季时显示 `EmptyState`

---

## 赛季首页 `/[seasonSlug]`

```
┌────────────────────────────────────────────┐
│  SeasonNav（tab 横滚，ScrollHint 渐变遮罩） │
├────────────────────────────────────────────┤
│  [StatusPill] 赛季种类                      │
│  赛季名（大标题）                            │
│                                            │
│  Phase Tracker（ScrollHint 横滚）           │
│  ┌──────┬──────┬──────┬──────┬──────┐      │
│  │  ✓   │  ✓   │ 当前 │      │      │      │
│  │REGST │VOTE  │DRAFT │PLAY  │FIN   │      │
│  └──────┴──────┴──────┴──────┴──────┘      │
│                                            │
│  NEXT MATCHES（仅 playing 阶段）            │
│  ┌────────────────┐┌────────────────┐      │
│  │ Team A vs B    ││ Team C vs D    │      │
│  │ 5月20日 / LIVE ││ 5月22日        │      │
│  └────────────────┘└────────────────┘      │
│                                            │
│  赛季导航（Quick Links，1/2/4 列响应）       │
│  ┌──────┐┌──────┐┌──────┐┌──────┐         │
│  │ 报名 ││ 投票 ││ 选秀 ││ 队伍 │...       │
│  └──────┘└──────┘└──────┘└──────┘         │
│                                            │
│  Stat 四格（2 列 / sm: 4 列）               │
│  TEAMS  PLAYERS  MATCHES  STAGE            │
└────────────────────────────────────────────┘
```

- Phase Tracker：capability 驱动（`hasCaptainVoting` / `hasDraft` / `stagePlan`），当前阶段 accent 色，已完成 ✓ 绿色
- NEXT MATCHES：`status === "playing"` 时显示，取最近 4 场 scheduled/in_progress，`formatCSTShortDate` 显示日期，LIVE 状态绿色 mono 标注
- Stat 四格：走 `<Stat>` 组件；MATCHES 格式 `finished/total`，无比赛时显示 `—`

---

## 报名页 `/[seasonSlug]/register`

```
┌────────────────────────────────────────────┐
│  报名 — 2026 春季选秀联赛                    │
│                                            │
│  各位置名额（实时）                           │
│  IGL [██████░░░░] 9/15                     │
│  AWP [████░░░░░░] 6/15                     │
│  ...                                       │
│                                            │
│  ─── 基础信息 ───                           │
│  邮箱      [                    ]          │
│  Steam64   [                    ]          │
│  QQ        [                    ]          │
│                                            │
│  ─── 位置 ───                              │
│  主选位置  [Select ▼]                      │
│  次选位置  [Select ▼]                      │
│                                            │
│  ─── 段位 ───                              │
│  历史最高  [        ] rating               │
│  截图      [上传 ↑]                        │
│                                            │
│  □ 愿意担任队长                             │
│  □ 我承诺不代打、不使用外挂                   │
│                                            │
│  [提交报名]                                │
└────────────────────────────────────────────┘
```

- 满员位置在下拉菜单中置灰 + 显示"已满"
- 截图区：NJUBox 分享链接输入；Rivals 默认 1 个链接，链接内包含近两周 5 场截图
- 提交按钮在 loading 时 disabled + spinner

---

## 队长投票页 `/[seasonSlug]/captains`

页面标题统一为 Eyebrow（mono 小标）+ 大标题 + 副标题三层结构。

**桌面端（≥ md）**：表格布局
```
┌────────────────────────────────────────────┐
│  [ S1 · CAPTAINS ]                         │
│  队长投票                                   │
│  我的票数：2/3  已投：[张三] [李四]           │
│                                            │
│  排行榜（实时）                              │
│  #  姓名   票数           操作             │
│  1  张三   ████████ 23   [已投] [撤回]     │
│  2  李四   ██████   17   [已投] [撤回]     │
│  3  王五   █████    12   [+投票]           │
└────────────────────────────────────────────┘
```

**移动端（< md）**：卡片列表（`CaptainVotingPanel` 自动切换）
```
┌───────────────────────────┐
│  张三              23票   │
│  ████████████████ 100%   │
│  [已投票] / [撤回]        │
├───────────────────────────┤
│  李四              17票   │
│  ████████████      74%   │
│  [+投票]                  │
└───────────────────────────┘
```

- 票数实时更新（Supabase Realtime）
- 移动端通过 `useMediaQuery("(max-width: 767px)")` 切换渲染模式

---

## 选秀直播间 `/[seasonSlug]/draft`

```
┌────────────────────────────────────────────┐
│  第 2 轮 / 第 5 选  ·  剩余 24 人           │
│  当前：Team Alpha  倒计时 [02:31]           │
│                                            │
│  队伍网格（TeamDraftGrid）                   │
│  桌面端（≥ md）：8 格横向网格，border-r 分隔  │
│  ┌────────┐ ┌────────┐ ┌────────┐          │
│  │Team A  │ │Team B* │ │Team C  │          │  ← * 当前选秀队高亮
│  │[队长]  │ │[队长]  │ │[队长]  │          │
│  │选手1   │ │???     │ │选手1   │          │
│  └────────┘ └────────┘ └────────┘          │
│                                            │
│  移动端（< md）：手风琴列表                  │
│  ▼ Team B（当前，自动展开）                  │
│    [队长] 张三  [选手] 李四                  │
│  ► Team A                                  │
│  ► Team C                                  │
│                                            │
│  剩余选手池（按位置分组）                     │
│  IGL: 张三 李四...  AWP: 王五...            │
└────────────────────────────────────────────┘
```

- 当前选秀队卡片高亮（theme_color 边框）
- 倒计时 < 30s 变红
- 移动端手风琴：当前选秀队自动展开，其余可点击展开/折叠（`expandedId` state）
- 选秀状态栏分隔线：`md:border-r`（移动端不显示贴边线）

---

## 队长面板 `/[seasonSlug]/draft/captain`（仅队长可见）

```
┌────────────────────────────────────────────┐
│  你的回合  剩余时间 [02:31]                  │
│                                            │
│  你的队伍：Team Alpha                       │
│  已选：[队长] [选手A] [选手B]                │
│  位置缺口：AWP × 1, Entry × 1              │
│                                            │
│  选手列表（可筛选位置）                       │
│  ┌──────────────────────────────────┐      │
│  │ 张三  IGL  Rating:8500  [选择]   │      │
│  │ 李四  AWP  Rating:9200  [选择]   │      │
│  └──────────────────────────────────┘      │
└────────────────────────────────────────────┘
```

- 非当前队长轮次时"选择"按钮 disabled
- 选择后立即 optimistic update，等待服务端确认

---

## 队伍列表 `/[seasonSlug]/teams`

```
8 支队伍卡片网格（4×2 / 移动端 2×4）
每卡片：队名 + 队长名 + 7 人头像列表（按位置图标）
点击跳转详情
```

## 队伍详情 `/[seasonSlug]/teams/[teamId]`

```
┌────────────────────────────────────────────┐
│  Team Alpha                                │
│  队长：张三（IGL）                           │
│                                            │
│  首发阵容                                   │
│  [IGL] 张三  Rating 8500                   │
│  [AWP] 李四  Rating 9200                   │
│  ...                                       │
│                                            │
│  替补                                       │
│  [Entry] 赵六  Rating 7800                 │
│  [Support] 钱七                            │
└────────────────────────────────────────────┘
```

---

## 赛程页 `/[seasonSlug]/matches`

```
┌────────────────────────────────────────────┐
│  双败淘汰赛 Bracket                          │
│  ┌──────── brackets-viewer 组件 ─────────┐ │
│  │  胜者组  │  败者组  │  总决赛          │ │
│  └─────────────────────────────────────┘  │
│                                            │
│  排位赛结果                                  │
│  2025-xx-xx  Team A 16:8 Team B  [详情]   │
│  ...                                       │
└────────────────────────────────────────────┘
```

---

## 比赛详情 `/[seasonSlug]/matches/[matchId]`

```
┌────────────────────────────────────────────┐
│  Team Alpha  vs  Team Beta                  │
│       16             14                    │
│                                            │
│  地图结果                                    │
│  Inferno   10:6  (Team Alpha)              │
│  Mirage     6:10 (Team Beta)               │
│  Nuke      16:14 (Team Alpha)              │
│                                            │
│  双方阵容（各 7 人）                          │
└────────────────────────────────────────────┘
```

---

## 管理后台

### `/admin/login`
标准居中登录卡片，invite code + password 两个输入框 + 登录按钮。

### `/admin/[seasonSlug]/registrations`
表格布局，列：邮箱 / 主选 / 段位 / 截图 / 状态 / 操作（通过/拒绝/等待）。

### `/admin/[seasonSlug]/captains`
两栏：左侧投票排行，右侧确认按钮组（选择前 8 名 + 手动排序 draft_order）。

### `/admin/[seasonSlug]/draft`
单页面：一键开始/暂停选秀 + 强制跳过当前队 + 实时日志。

### `/admin/[seasonSlug]/matches`
比赛列表 + 录入比分表单（两个 number input + 提交按钮）。

---

## 移动端断点策略

- `sm`（640px）：导航折叠为 hamburger
- `md`（768px）：报名表单单列 → 双列
- `lg`（1024px）：选秀直播间 4 列队伍网格
- 全站最小宽度：320px
