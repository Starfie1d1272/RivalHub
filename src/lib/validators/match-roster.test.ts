import { describe, it } from "vitest";

describe("validateRosterSubmission", () => {
  it.todo("accepts exactly 5 starters");
  it.todo("rejects when team member belongs to wrong team");
  it.todo("rejects when less than 2h before match");
});
