import { describe, it } from "vitest";

describe("validateTimeProposal", () => {
  it.todo("accepts valid time within window");
  it.todo("rejects time earlier than minimum allowed");
  it.todo("rejects time later than stage deadline");
});
